type t = {
  types : (cl_uid * cl_type) list;
  vars : (cl_uid * var) list;
  fncs : (cl_uid * fnc) list;
  (* callgraph *)
}

type cl_uid <ocaml from="Loc" t="cl_uid"> = abstract

type cl_type <ocaml from="Type" t="t"> = abstract

type var <ocaml from="Var" t="t"> = abstract

type fnc <ocaml from="Fnc" t="t"> = abstract
